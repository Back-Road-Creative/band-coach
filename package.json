{
  "name": "band-coach",
  "version": "0.1.0",
  "private": true,
  "description": "Adaptive music-instrument coach that runs from a single HTML file",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node build/build.mjs",
    "pretest": "npm run build",
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
